# 快速开始指南

欢迎使用线性代数计算器（LAC）！本指南帮助你快速上手。

## 安装

### Windows（最简单）

1. 下载 `LAC-v1.0.0-Windows.zip`
2. 右键解压或用 WinRAR、7-Zip 解压
3. 打开解压后的文件夹
4. **双击** `lac_cli_app.exe` 启动程序

---

## 第一次使用

启动程序后，你会看到类似这样的提示：

```
>>> 
```

### 例子 1：简单加法

```
>>> 1 + 2
... :
Matrix 'Ans' (1x1):
3
```

**说明**：
- 输入表达式 `1 + 2`
- 按回车，再输入单独的 `:` 表示计算完成
- 结果 `Ans = 3` 自动保存在变量 `Ans` 中

### 例子 2：矩阵运算

```
>>> A = [ 1 2 ; 3 4 ]
... :
Matrix 'Ans' (2x2):
1 2
3 4

>>> A + A
... :
Matrix 'Ans' (2x2):
2 4
6 8
```

**说明**：
- `[ 1 2 ; 3 4 ]` 表示矩阵，`;` 分隔行，空格分隔列
- 给矩阵取名 `A`，后续可直接使用
- `A + A` 计算矩阵和

### 例子 3：矩阵乘法

```
>>> B = [ 1 0 ; 0 2 ]
... :
Matrix 'Ans' (2x2):
1 0
0 2

>>> A * B
... :
Matrix 'Ans' (2x2):
1  4
3  8
```

---

## 常用命令

以冒号 `:` 开头输入命令：

```
: help
```
显示所有可用命令

```
: memory
```
查看已保存的所有矩阵

```
: lookup A
```
查看矩阵 A 的值

```
: rename A MyMatrix
```
把矩阵 A 重命名为 MyMatrix

```
: delete A
```
删除矩阵 A

```
: clear
```
删除所有保存的矩阵

```
: exit
```
退出程序

---

## 常见问题

**Q：多行表达式怎么输入？**

A：输入多行后，单独输入一行 `:` 结束：

```
>>> A = [
... 1 2 3 ;
... 4 5 6
... ]
... :
Matrix 'Ans' (2x3):
1 2 3
4 5 6
```

**Q：所有 token 都要用空格分隔吗？**

A：是的。正确格式：`[ 1 2 ; 3 4 ]`，错误格式：`[1 2;3 4]`

**Q：程序崩溃了怎么办？**

A：查看错误提示，确认：
- 矩阵维度是否匹配（乘法要求 A 的列数 = B 的行数）
- 是否有奇异矩阵（行列式为 0 时无法求逆）
- 变量名是否拼写正确

如问题持续，请提交 Issue 或者联系我。

---

## 下一步

- 详细功能说明见 [README.md](README.md)
